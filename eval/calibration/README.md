# Independent human calibration pending

Copy five successfully generated, real-model rows from one pilot's `review-template.csv` into two independent reviewer files. Each person reads the exact source and conversation and fills the sheet without seeing the other's scores. Do not count fixed application-only quiz redirects as the five model outputs.

Use `eval/calibrate.py` to compare them. Original and revised disagreements remain evidence; do not backfill perfect agreement. No completed human score sheet is supplied by this implementation.
